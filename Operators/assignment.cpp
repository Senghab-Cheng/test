#include <iostream>
using namespace std;
int main(){
    system(" clear ");
    // int x = 5;
    // x = x + 1; // full hand
    // x += 1; // short hand
    // x -= 2; 
    // x * 3; // will not change x

    // cout << "x = " << x * 3 << endl;
    system(" clear");

    int y = 10;
    // y++; // y = y + 1
    // ++y; // y = y + 1
    int b = 5;
    // b = y++; // y = y +1
    b = ++y;
    cout << "y = " << y << endl;
    cout << " b = " << b << endl;

     return 0;
}